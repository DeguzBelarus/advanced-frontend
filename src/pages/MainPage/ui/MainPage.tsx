import { FC } from 'react';

import styles from './MainPage.module.scss';

const MainPage: FC = () => {
  return <div className={styles['main-page-wrapper']}>
    <h1>MainPage works</h1>
  </div>
};

export default MainPage;